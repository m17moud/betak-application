import 'package:flutter/material.dart';

class OnBoardingSecondaryView extends StatelessWidget {
  const OnBoardingSecondaryView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('saddas'),),
     body: Container(),
    );
  }
}
