import 'package:betak/features/auth_for_client/sign_up/presentation/views/widgets/client_password_view_body.dart';
import 'package:flutter/material.dart';

class ClientPasswordView extends StatelessWidget {
  const ClientPasswordView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: ClientPasswordViewBody(),
    );
  }
}
